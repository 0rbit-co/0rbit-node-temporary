// this function will send a response to ao
import { connect, createDataItemSigner } from '@permaweb/connect'

const walletFile = process.env.WALLET_FILE || './wallet.json'

export async function send(target, data, tags) {

}